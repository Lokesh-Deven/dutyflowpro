---
trigger: always_on
---

# Git Commit and Push Policy

- **DO NOT** run `git commit` or `git push` automatically after making code changes.
- **ONLY** commit and push to Git when explicitly instructed or prompted by the user in their message (e.g., "please commit", "push to github", etc.).
